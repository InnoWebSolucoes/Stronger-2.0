import { Text, View } from 'react-native';

export default function Boot() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ color: '#F5A524', fontSize: 24, fontWeight: '700' }}>Stronger</Text>
    </View>
  );
}
